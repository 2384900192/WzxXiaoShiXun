<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:layout_height="130dp"
        android:layout_width="130dp"
        android:scaleType="centerCrop"
        android:layout_marginTop="40dp"
        android:id="@+id/iv_dl_header"
        android:layout_gravity="center"
        android:background="@drawable/adc"/>

    <TextView android:layout_height="wrap_content"
        android:layout_width="match_parent" android:layout_marginTop="20dp" android:id="@+id/tv_dl_header" android:textSize="18sp" android:textColor="@color/colorPrimary" android:gravity="center"
        android:text="登录"/>
</LinearLayout>